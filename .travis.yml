language: python
python:
  - "2.7"

before_install: "pip install -r requeriments.txt"

# script: "python manage.py test"

script:
    - "coverage run --source tests.py test"

after_script:
    "coveralls"
